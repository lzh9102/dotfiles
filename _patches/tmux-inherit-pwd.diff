diff --git a/tmux.conf b/tmux.conf
index ecfa8df..db6acdc 100644
--- a/tmux.conf
+++ b/tmux.conf
@@ -21,7 +21,7 @@ bind-key -r C-p previous-window
 bind-key Tab last-window
 
 # open a pane that occupies 1/4 height of the window
-bind-key e split-window -v -p 25
+bind-key e split-window -v -p 25 -c "#{pane_current_path}"
 
 bind-key -t vi-copy 'v' begin-selection
 bind-key -t vi-copy 'y' copy-selection
@@ -31,6 +31,10 @@ bind-key C-v run "xclip -o -sel clipboard | tmux load-buffer -; tmux paste-buffe
 # word separators in copy mode
 setw -g word-separators " ~!@#$%^&*()-+=|\\[]{}'\";:/?.>,<"
 
+bind '"' split-window -c "#{pane_current_path}"
+bind % split-window -h -c "#{pane_current_path}"
+bind c new-window -c "#{pane_current_path}"
+
 # vim-like keybindings {{{
 
 setw -g mode-keys vi
